meta {
  name: Page Load
  type: http
  seq: 1
}

get {
  url: {{directViteUrl}}/
  body: none
  auth: none
}

assert {
  res.status: eq 200
}

tests {
  test("should load Vite frontend", function() {
    expect(res.getStatus()).to.equal(200);
    expect(res.getBody()).to.include("Subnet Calculator");
  });
}
