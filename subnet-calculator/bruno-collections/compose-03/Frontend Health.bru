meta {
  name: Frontend Health
  type: http
  seq: 1
}

get {
  url: {{compose03FrontendUrl}}/
  body: none
  auth: none
}

assert {
  res.status: eq 200
}

tests {
  test("should load Flask frontend", function() {
    expect(res.getStatus()).to.equal(200);
    expect(res.getBody()).to.include("Subnet Calculator");
  });
}
