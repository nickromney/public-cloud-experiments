# Azure Deployment Environment Configuration
#
# This file contains environment variables needed for subnet-calculator
# Azure deployments. Copy this to .env and fill in your values.
#
# IMPORTANT: .env is excluded from version control to prevent
# accidentally checking in sensitive data like subscription IDs,
# resource groups, and API credentials.
#
# Setup:
#   1. brew install direnv
#   2. Add to ~/.bashrc or ~/.zshrc:
#      eval "$(direnv hook bash)"  # or hook zsh
#   3. cd subnet-calculator/infrastructure/azure
#   4. cp .env.example .env
#   5. Edit .env with your values
#   6. direnv allow
#   7. Variables are now automatically loaded when you cd to this directory
#
# See: https://direnv.net/
#

# Azure Subscription and Resource Group
# Get these from: az account show --query "{subscriptionId:id,displayName:name}"
# and: az group list --query "[].[name,location]"
RESOURCE_GROUP=rg-subnet-calc
LOCATION=uksouth

# Azure AD / Service Principal Credentials (optional, for automated CI/CD)
# These are needed for unattended deployments
# Get these from: az ad app create --display-name "subnet-calc-deploy"
AZURE_CLIENT_ID=
AZURE_CLIENT_SECRET=
AZURE_TENANT_ID=
AZURE_SUBSCRIPTION_ID=

# API Management (APIM) Configuration
# For API Management deployments, provide a contact email
PUBLISHER_EMAIL=your-email@example.com

# Function App Configuration (optional overrides)
# These are auto-detected by default, but you can explicitly set them
FUNCTION_APP_NAME=
STATIC_WEB_APP_NAME=

# Terraform / OpenTofu Variables (optional)
# These are used by terraform/claranet-tfwrapper deployments
# Note: These will use the values from AZURE_* above
ARM_CLIENT_ID=$AZURE_CLIENT_ID
ARM_CLIENT_SECRET=$AZURE_CLIENT_SECRET
ARM_TENANT_ID=$AZURE_TENANT_ID
ARM_SUBSCRIPTION_ID=$AZURE_SUBSCRIPTION_ID
