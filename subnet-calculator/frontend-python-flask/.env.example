# Flask Frontend Environment Configuration
#
# This file contains environment variables for the Flask frontend.
# Copy this to .env and fill in your values.
#
# IMPORTANT: .env is excluded from version control to prevent
# accidentally checking in sensitive data like API credentials,
# session keys, and client secrets.
#
# Setup with direnv:
#   1. brew install direnv
#   2. Add to ~/.bashrc or ~/.zshrc:
#      eval "$(direnv hook bash)"  # or hook zsh
#   3. cd subnet-calculator/frontend-python-flask
#   4. cp .env.example .env
#   5. Edit .env with your values
#   6. direnv allow
#   7. Variables are now automatically loaded when you cd to this directory
#
# See: https://direnv.net/
#

# === Entra ID Authentication (Optional) ===
# Leave blank to disable authentication
# Get these from your Entra ID app registration:
#   - AZURE_CLIENT_ID: Application (client) ID
#   - AZURE_CLIENT_SECRET: Client secret value
#   - AZURE_TENANT_ID: Directory (tenant) ID
#
# Get AZURE_TENANT_ID with: az account show --query tenantId -o tsv
# Get AZURE_CLIENT_ID/SECRET from: Azure Portal → Entra ID → App registrations

AZURE_CLIENT_ID=
AZURE_CLIENT_SECRET=
AZURE_TENANT_ID=

# OAuth callback redirect URI
# For local development: http://localhost:5000/auth/callback
# For production: https://your-app-service.azurewebsites.net/auth/callback
# This MUST match the redirect URI registered in your app registration
REDIRECT_URI=http://localhost:5000/auth/callback

# === Flask Configuration ===

# Secret key for session encryption
# For development: Use any value (app.py has a dev default)
# For production: Generate with: openssl rand -hex 32
# WARNING: Change this for each deployment!
FLASK_SECRET_KEY=dev-key-change-in-production

# Flask environment
# development: Debug mode, auto-reload, detailed errors
# production: Secure cookies, no debug output
FLASK_ENV=development

# === Backend API Configuration ===

# Base URL for the backend API
# Examples:
#   http://localhost:7071/api/v1           (Azure Function local)
#   http://localhost:8090/api/v1            (Container App local)
#   https://func-api.azurewebsites.net/api/v1  (Azure Function production)
API_BASE_URL=http://localhost:7071/api/v1

# Display name for the stack (shown in UI)
STACK_NAME=Python Flask + Entra ID

# === Optional: JWT Backend Authentication ===
# Only used if backend requires JWT authentication
# Leave blank for public backends

# Backend API username (for JWT login)
JWT_USERNAME=

# Backend API password (for JWT login)
JWT_PASSWORD=
