apiVersion: v1
metadata:
  name: apim-simulator
  labels:
    app.kubernetes.io/name: apim-simulator
    app.kubernetes.io/component: gateway
kind: Service
spec:
  selector:
    app.kubernetes.io/name: apim-simulator
    app.kubernetes.io/component: gateway
  ports:
    - name: http
      port: 8000
      targetPort: http
