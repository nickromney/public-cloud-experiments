apiVersion: v1
kind: Service
metadata:
  name: apim-sentiment
  namespace: azure-apim-sim
  labels:
    app.kubernetes.io/name: apim-sentiment
    app.kubernetes.io/component: gateway
spec:
  selector:
    app.kubernetes.io/name: apim-sentiment
    app.kubernetes.io/component: gateway
  ports:
    - name: http
      port: 8000
      targetPort: 8000
