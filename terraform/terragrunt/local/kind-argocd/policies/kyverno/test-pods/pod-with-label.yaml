# Test pod that should PASS the require-labels policy (has team label)
apiVersion: v1
kind: Pod
metadata:
  name: test-with-label
  namespace: kyverno-sandbox
  labels:
    team: platform
spec:
  containers:
    - name: nginx
      image: nginx:alpine
  restartPolicy: Never
