# Test pod that should FAIL the require-labels policy (missing team label)
apiVersion: v1
kind: Pod
metadata:
  name: test-no-label
  namespace: kyverno-sandbox
spec:
  containers:
    - name: nginx
      image: nginx:alpine
  restartPolicy: Never
