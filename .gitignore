# Local .terraform directories
.terraform/

# .tfstate files
*.tfstate
*.tfstate.*

# Crash log files
crash.log
crash.*.log

# Exclude all .tfvars files, which are likely to contain sensitive data, such as
# password, private keys, and other secrets. These should not be part of version
# control as they are data points which are potentially sensitive and subject
# to change depending on the environment.
*.tfvars
*.tfvars.json

# Ignore override files as they are usually used to override resources locally and so
# are not checked in
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Ignore transient lock info files created by terraform apply
.terraform.tfstate.lock.info

# Include override files you do wish to add to version control using negated pattern
# !example_override.tf

# Include tfplan files to ignore the plan output of command: terraform plan -out=tfplan
# example: *tfplan*

# Ignore CLI configuration files
.terraformrc
terraform.rc

# Ignore reference repositories (cloned for documentation)
terraform/reference/

# Ignore tfwrapper's Azure CLI cache directory
terraform/claranet-tfwrapper/.run/

# tfwrapper state.tf files (contain subscription IDs, tenant IDs, potentially client secrets)
terraform/claranet-tfwrapper/**/state.tf

# Terragrunt cache directories
.terragrunt-cache/

# Terragrunt generated files (contain subscription IDs, tenant IDs, resource groups)
# These are auto-generated from root.hcl and should not be committed
terraform/terragrunt/**/backend.tf
terraform/terragrunt/**/provider.tf
terraform/terragrunt/**/versions.tf

# direnv configuration (may contain actual subscription IDs)
.envrc

# Environment variable files (may contain sensitive data like subscription IDs, client secrets)
.env
.env.local
*.env.local

# tflint configuration files (auto-generated by Makefile)
.tflint.hcl

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.venv/
venv/
ENV/
*.egg-info/
dist/
build/

# Node.js
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
package-lock.json
yarn.lock
pnpm-lock.yaml

# Vite
dist/
dist-ssr/
*.local

# TypeScript
*.tsbuildinfo

# Playwright
playwright-report/
test-results/
*.trace

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db
